SQLNET.AUTHENTICATION_SERVICES = (TCPS)
# FALSE allows both TLS and mTLS
SQLNET.SSL_CLIENT_AUTHENTICATION=TRUE

WALLET_LOCATION =
  (SOURCE =
    (METHOD = FILE)
    (METHOD_DATA =
      (DIRECTORY = /root/client_wallet)
    )
  )
