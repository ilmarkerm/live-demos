NAMES.DIRECTORY_PATH= (TNSNAMES, EZCONNECT, HOSTNAME)
DISABLE_OOB=ON
SQLNET.EXPIRE_TIME=3

SQLNET.AUTHENTICATION_SERVICES = (BEQ, TCPS)

# Make mTLS optional
# It cannot be FALSE if need to authenticate also with certificates
TLS_CLIENT_AUTHENTICATION = OPTIONAL
